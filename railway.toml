[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npm install && pip install yt-dlp && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
